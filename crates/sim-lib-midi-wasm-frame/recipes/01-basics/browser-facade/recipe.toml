id = "frame-facade"
title = "MIDI frame facade"
codec = "lisp"
setup = "setup.siml"
purpose = "purpose.md"
order = 10
tags = ["midi", "wasm", "facade", "sandbox-descriptor"]
requires = ["midi-wasm", "codec/lisp"]
