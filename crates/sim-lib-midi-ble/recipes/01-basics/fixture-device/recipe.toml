id = "fixture-device"
title = "BLE fixture device"
codec = "lisp"
setup = "setup.siml"
purpose = "purpose.md"
order = 10
tags = ["midi", "ble", "fixture", "sandbox-descriptor"]
requires = ["midi-ble", "codec/lisp"]
