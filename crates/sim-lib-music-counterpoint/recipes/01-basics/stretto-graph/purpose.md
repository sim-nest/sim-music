# Map Bounded Stretto Relations

The result preserves the catalog's couple and chain relations as inspectable
graph data. `entries` are bounded transform candidates, `couples` are compatible
edges, `cliques` require every pair to be compatible, and `chains` use the
largest normalized suffix/prefix overlap. The report says `generation=false`;
fusion is only a derived counterpoint view.
