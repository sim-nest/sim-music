[package]
name = "sim-lib-pitch-scale"
version = "0.4.0"
edition = "2024"
license = "MPL-2.0"
description = "Scale, key, mode, and degree mapping helpers for pitch-domain transforms."
readme = "README.md"
publish = true
repository = "https://github.com/sim-nest/sim-music"
homepage = "https://github.com/sim-nest/sim-music"

[dependencies]
sim-cookbook = { version = "0.4.0" }
sim-lib-discrete-search = { version = "0.3.0" }
sim-lib-pitch-core = { version = "0.4.0", path = "../sim-lib-pitch-core" }
sim-lib-pitch-set = { version = "0.4.0", path = "../sim-lib-pitch-set" }
thiserror = "1"

[build-dependencies]
sim-cookbook-build = { version = "0.2.0" }
