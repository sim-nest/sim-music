id = "packetize"
title = "MIDI packet descriptor"
codec = "lisp"
setup = "setup.siml"
purpose = "purpose.md"
order = 10
tags = ["stream", "midi", "packet", "sandbox-descriptor"]
requires = ["stream-midi", "codec/lisp"]
