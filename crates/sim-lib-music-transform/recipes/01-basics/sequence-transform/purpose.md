# Sequence Transform (descriptor)

Documents sequence transform in the music domain, including pitch-map policies for explicit holes, nudges, rejections, and inverse witnesses. Music sequencing, synthesis, and notation run through the audio and render pipeline outside the cookbook sandbox eval stack, so this is documented rather than run in the cookbook, whose sandbox eval stack loads only core construction and math over the number domains.
