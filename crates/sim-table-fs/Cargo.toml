[package]
name = "sim-table-fs"
version = "0.1.0"
edition = "2024"
license = "MPL-2.0"
description = "SIM workspace package for sim table fs."
readme = "README.md"
publish = true
repository = "https://github.com/sim-nest/sim-music"
homepage = "https://github.com/sim-nest/sim-music"

[dependencies]
sim-citizen = { version = "0.1.0" }
sim-citizen-derive = { version = "0.1.0" }
sim-codec = { version = "0.1.0" }
sim-kernel = { version = "0.1.0" }
sim-lib-midi-shapes = { version = "0.1.0", optional = true, path = "../sim-lib-midi-shapes" }
sim-lib-midi-smf = { version = "0.1.0", optional = true, path = "../sim-lib-midi-smf" }
sim-lib-music-core = { version = "0.1.0", optional = true, path = "../sim-lib-music-core" }
sim-lib-music-notation = { version = "0.1.0", optional = true, path = "../sim-lib-music-notation" }
sim-lib-music-shapes = { version = "0.1.0", optional = true, path = "../sim-lib-music-shapes" }
sim-lib-pitch-core = { version = "0.1.0", optional = true, path = "../sim-lib-pitch-core" }
sim-lib-sound-core = { version = "0.1.0", optional = true, path = "../sim-lib-sound-core" }
sim-lib-sound-shapes = { version = "0.1.0", optional = true, path = "../sim-lib-sound-shapes" }
sim-lib-sound-tuning = { version = "0.1.0", optional = true, path = "../sim-lib-sound-tuning" }
sim-table-core = { version = "0.1.0" }

[dev-dependencies]
sim-codec-algol = { version = "0.1.0" }
sim-codec-binary = { version = "0.1.0" }
sim-codec-binary-base64 = { version = "0.1.0" }
sim-codec-chat = { version = "0.1.0" }
sim-codec-json = { version = "0.1.0" }
sim-codec-lisp = { version = "0.1.0" }
sim-kernel = { version = "0.1.0", features = ["test-support"] }
sim-test-support = { version = "0.1.0" }

[features]
default = []
midi = ["dep:sim-lib-midi-shapes", "dep:sim-lib-midi-smf"]
music = ["dep:sim-lib-music-core", "dep:sim-lib-music-shapes"]
notation = ["dep:sim-lib-music-core", "dep:sim-lib-music-notation", "dep:sim-lib-music-shapes"]
sound = ["dep:sim-lib-sound-core", "dep:sim-lib-sound-shapes"]
tuning = ["dep:sim-lib-pitch-core", "dep:sim-lib-sound-core", "dep:sim-lib-sound-shapes", "dep:sim-lib-sound-tuning"]

[lints]
workspace = true
