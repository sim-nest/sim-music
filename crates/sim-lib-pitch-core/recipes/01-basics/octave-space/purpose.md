# Octave Space Folding (descriptor)

Documents reusable octave-space folding in the pitch domain. The pitch and harmony-theory
classes are not among the domains loaded in the cookbook sandbox eval stack, so this is
documented rather than run in the cookbook, whose sandbox eval stack loads only core
construction and math over the number domains.
