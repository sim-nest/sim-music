# Exact Ratio (descriptor)

Documents exact musical ratio intervals. The ratio crate is pure Rust and the
cookbook sandbox does not load the pitch-ratio runtime as a Lisp class, so this
recipe records the descriptor shape used by generated docs and the Index.
