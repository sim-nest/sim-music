# Release-plz computes dependency updates and release order for this workspace.
# Dev harnesses use publish=false in their Cargo.toml and are skipped
# automatically.
[workspace]
# One version for the music crate group; bump together.
dependencies_update = true
git_release_enable = true
# Publish crates in dependency order (release-plz computes the order).
